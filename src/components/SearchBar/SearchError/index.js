import React from "react";
import "./SearchError.css";

const SearchError = () => {
  return (
    <span className="c-SearchError">Please type the location again...</span>
  );
};

export default SearchError;
